pipeline {
    agent any
    tools {
        nodejs 'nodejs'
    }

    stages {

        stage("testVersion") {
            steps {
                echo "🚀 Checking node version "
                sh ' node --version '
            }
        }
  
        }
    }
}
